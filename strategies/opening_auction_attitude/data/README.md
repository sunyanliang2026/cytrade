# Strategy-owned data

Use this directory for OpeningAuctionAttitude-specific input files, fixtures, and samples.

Large runtime artifacts should stay out of git unless explicitly curated as small fixtures.
